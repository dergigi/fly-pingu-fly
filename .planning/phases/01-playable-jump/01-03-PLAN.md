---
phase: 01-playable-jump
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - package.json
  - public/assets/sprites/README.md
  - scripts/check-assets.mjs
  - scripts/check-assets.test.ts
  - src/game/penguinFrames.ts
  - src/game/penguinFrames.test.ts
  - src/scenes/PlayScene.ts
autonomous: true
requirements:
  - INPT-03
  - PRES-01
  - PRES-02
  - PRES-03
must_haves:
  truths:
    - "INPT-03 presentation: an accepted takeoff selects its takeoff/airborne frame on the first rendered snapshot after the authoritative transition."
    - "PRES-01: the finished scene is bright and snowy, uses only `winter-forest.webp` and `snow-pile.webp` from the curated raster scenery, and keeps the ramp lip plus takeoff stripe visible before the decision."
    - "PRES-02: ramp framing shows the lip ahead, flight framing leads toward landing terrain, and slide framing settles with the resting penguin; all camera targets are derived read-only from phase snapshots."
    - "PRES-03: the committed 640x240 RGBA `sprite_penguin.png` at the required path is the approved Where's My Egg? sheet, and every selected pose has an explicit crop plus contact offset."
    - "PRES-03 alignment: switching among ramp, takeoff, flight, landing, slide, and resting poses preserves the same world-space snow-contact point."
    - "`npm run build` invokes `prebuild`, and `prebuild` exits non-zero before Vite starts if a required art path is missing, empty, structurally undecodable, has the wrong PNG/WebP signature, or the penguin sheet is not exactly 640x240."
  artifacts:
    - path: scripts/check-assets.mjs
      provides: "Build-blocking exact-path, signature, decodable image-metadata, and 640x240 sprite-sheet validation"
      exports: ["checkRequiredAssets"]
    - path: scripts/check-assets.test.ts
      provides: "Fixture-based proof that missing, empty, invalid-signature, and ready asset sets return the correct process status"
    - path: public/assets/sprites/sprite_penguin.png
      provides: "Approved player character sheet required by D-05 and D-07"
    - path: public/assets/sprites/README.md
      provides: "Source, permission statement, selected-use record, and file inventory"
    - path: src/game/penguinFrames.ts
      provides: "Typed frame crops, phase mapping, and contact offsets"
      exports: ["PenguinPose", "PenguinFrame", "PENGUIN_FRAMES", "poseForJumpPhase"]
    - path: src/scenes/PlayScene.ts
      provides: "Contact-anchored sprite, curated scenery, and phase-aware camera"
  key_links:
    - from: package.json
      to: scripts/check-assets.mjs
      via: "prebuild runs the readiness script before Vite build"
      pattern: "\"prebuild\""
    - from: src/scenes/PlayScene.ts
      to: src/game/penguinFrames.ts
      via: "snapshot phase selects a frame and applies its contact offset"
      pattern: "(PENGUIN_FRAMES|poseForJumpPhase)"
    - from: src/scenes/PlayScene.ts
      to: public/assets/sprites/sprite_penguin.png
      via: "Phaser loader uses the root-relative required path"
      pattern: "/assets/sprites/sprite_penguin\\.png"
    - from: src/scenes/PlayScene.ts
      to: src/game/jump.ts
      via: "sprite and camera read JumpState snapshots without mutating them"
      pattern: "JumpState"
  prohibitions:
    - statement: "The selected player character must not be replaced by `snowball-penguin.webp` or any other penguin art."
      status: unverified
      verification: judgment
    - statement: "Fantasy, animal, vegetation, pine-tree, and rock-cluster raster assets outside the two-file curated scenery must not be loaded."
      status: unverified
      verification: judgment
    - statement: "Sprite frame dimensions, transparent padding, pose offsets, and camera motion must not alter collision or scored simulation facts."
      status: unverified
      verification: judgment
---

<objective>
Replace the geometric marker with the approved contact-anchored penguin and finish the readable snow presentation.

Purpose: The complete deterministic jump must be visually understandable and aligned without letting art or camera behavior change gameplay.
Output: Verified committed art inputs, provenance record, frame manifest, contact-anchored Phaser rendering, curated snow scenery, and phase-aware camera.

Decision coverage: D-05 public asset organization, D-06 snow level, D-07 exact character choice, and D-08 small curated asset subset.

Scope rationale: The seven modified files form one cohesive final presentation slice. The build gate, manifest, tests, and renderer operate against the three committed art inputs as one asset contract; separating them would allow a build or scene integration to pass without proving the exact required art inputs.
</objective>

## Phase Goal

**As a** young child, **I want to** complete a full ski jump with one well-timed action, **so that** I can immediately understand and enjoy the jump.

<execution_context>
@/Users/gigi/Development/vibe/fly-pingu-fly/.cursor/gsd-core/workflows/execute-plan.md
@/Users/gigi/Development/vibe/fly-pingu-fly/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-playable-jump/01-CONTEXT.md
@.planning/phases/01-playable-jump/01-RESEARCH.md
@.planning/phases/01-playable-jump/01-PATTERNS.md
@.planning/phases/01-playable-jump/01-02-SUMMARY.md

<interfaces>
Consume:
- `JumpState` and its `phase`, `(x, y)`, velocity, and sampled-surface fields
- simulation `(x, y)` as the immutable snow-contact point

Create:
- `type PenguinPose = "ramp" | "takeoff" | "flight" | "landing" | "slide" | "resting"`
- `type PenguinFrame = { x: number; y: number; width: number; height: number; contactX: number; contactY: number }`
- `const PENGUIN_FRAMES: Record<PenguinPose, PenguinFrame>`
- `poseForJumpPhase(state: JumpState): PenguinPose`
- `checkRequiredAssets(rootDir?: string): void` in `scripts/check-assets.mjs`
- package script `"prebuild": "node scripts/check-assets.mjs"`
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove the committed art contract through prebuild</name>
  <read_first>
    - public/assets/sprites/sprite_penguin.png
    - public/assets/sprites/winter-forest.webp
    - public/assets/sprites/snow-pile.webp
    - .planning/phases/01-playable-jump/01-CONTEXT.md
    - .planning/phases/01-playable-jump/01-RESEARCH.md
    - .planning/phases/01-playable-jump/01-PATTERNS.md
  </read_first>
  <files>package.json, public/assets/sprites/README.md, scripts/check-assets.mjs, scripts/check-assets.test.ts</files>
  <behavior>
    - Test 1: the committed files at the three exact required paths pass readiness validation.
    - Test 2: a missing path, empty file, wrong PNG/WebP signature, malformed or truncated image container, or unreadable image dimensions fails readiness and identifies every bad path.
    - Test 3: `sprite_penguin.png` must decode its PNG metadata as exactly 640x240; any other positive dimensions fail.
    - Test 4: package.json wires `prebuild` to the readiness script so `npm run build` cannot start Vite first.
  </behavior>
  <action>Write the failing readiness tests first, using copies of the committed images as valid fixtures and targeted mutations for each failure class. Implement `checkRequiredAssets(rootDir)` plus direct execution in `scripts/check-assets.mjs`. Resolve only `public/assets/sprites/sprite_penguin.png`, `winter-forest.webp`, and `snow-pile.webp` beneath the supplied root per D-05 through D-08. Validate non-empty files, exact PNG or RIFF/WEBP signatures, complete parseable PNG/WebP container metadata with finite positive dimensions, and an exact 640x240 PNG size for the approved RGBA penguin sheet. Collect all errors before exiting non-zero. Add `prebuild` ahead of the existing Vite build. Record the selected sheet source, user's stated permission, verified dimensions, the two lean scenery uses, and the unused-art policy in `public/assets/sprites/README.md`; explicitly record that `snowball-penguin.webp` cannot substitute for the D-07 character.</action>
  <verify>
    <automated>npm test -- --run scripts/check-assets.test.ts &amp;&amp; node scripts/check-assets.mjs</automated>
  </verify>
  <acceptance_criteria>The committed approved sheet and two selected snow assets pass exact-path, format, structural decodability, and dimension checks; every tested corruption fails before Vite starts.</acceptance_criteria>
  <done>The production build has a tested end-to-end readiness gate for the real committed art set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Anchor approved poses and frame each jump phase</name>
  <read_first>
    - public/assets/sprites/sprite_penguin.png
    - public/assets/sprites/winter-forest.webp
    - public/assets/sprites/snow-pile.webp
    - src/game/jump.ts
    - src/scenes/PlayScene.ts
    - .planning/phases/01-playable-jump/01-RESEARCH.md
  </read_first>
  <files>src/game/penguinFrames.ts, src/game/penguinFrames.test.ts, src/scenes/PlayScene.ts</files>
  <behavior>
    - Test 1: every PenguinPose maps to a finite positive crop fully inside the received sheet.
    - Test 2: every pose defines finite contactX/contactY offsets.
    - Test 3: applying each pose offset at the same simulation point yields the same world snow-contact coordinate.
    - Test 4: every JumpPhase and the accepted-takeoff transition map to a documented pose.
    - Test 5: every crop is bounded by the verified 640x240 sheet dimensions and uses a visibly occupied region selected from sheet inspection.
  </behavior>
  <action>Write the failing frame-manifest tests first. Inspect the committed 640x240 sheet itself at usable zoom, identify the visible occupied bounds of the chosen ramp, takeoff, flight, landing, slide, and resting poses, and record exact crop rectangles rather than assuming a uniform grid. For each chosen crop, identify the stable foot, belly, or sled-to-snow contact pixel appropriate to that pose and encode its `contactX`/`contactY` offset in `penguinFrames.ts`. Map every jump phase plus the accepted-takeoff transition to those inspected poses and prove crop bounds and invariant world contact in tests. Register the explicit crops with Phaser, place the sprite so simulation `(x, y)` remains the snow-contact coordinate, align ramp/slide rotation to the terrain tangent, and use clamped velocity-derived rotation in flight. Replace the geometric marker only after the approved sheet loads successfully. Load `winter-forest.webp` sparsely in the background and one `snow-pile.webp` accent per D-06 and D-08; load no other raster scenery, and never substitute `snowball-penguin.webp` per D-07. Implement camera targets by phase: lead toward the visible lip on ramp, lead toward landing terrain in flight, then settle and stop at rest. Keep all pose, origin, crop, rotation, and camera calculations read-only with respect to `JumpState`.</action>
  <verify>
    <automated>npm test -- --run scripts/check-assets.test.ts src/game/penguinFrames.test.ts src/game/jump.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
    <human-check>Run `npm run dev` and complete early, ideal, late, and no-input attempts. Confirm the selected penguin visibly changes pose at takeoff, stays attached to the ramp/landing surface at its contact point, never clips below snow at first contact, and remains framed with the lip or landing area visible at the relevant phase.</human-check>
  </verify>
  <acceptance_criteria>The approved character sheet renders for all phases with stable contact alignment; only the two selected snow scenery files load; camera framing keeps the next decision/landing area readable; missing, empty, or incorrectly encoded required art blocks prebuild; tests, typecheck, and production build pass.</acceptance_criteria>
  <done>`PRES-01`, `PRES-02`, and `PRES-03` are satisfied with the chosen art, the build enforces asset readiness, and the complete jump remains deterministic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| committed art -> static bundle | Binary image inputs and their provenance enter the published application. |
| frame manifest -> renderer | Crop and offset metadata controls what is rendered but must not cross into gameplay authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-08 | Repudiation | art provenance | medium | mitigate | Record source URL, user's permission statement, selected files, and usage in `public/assets/sprites/README.md` before publication. |
| T-01-09 | Tampering | `scripts/check-assets.mjs` and image inputs | medium | mitigate | `prebuild` rejects missing, empty, structurally malformed, signature-mismatched, or wrongly sized required files before Vite; fixture tests cover every failure mode and frame tests enforce 640x240 crop bounds. |
| T-01-10 | Tampering | frame/contact metadata | medium | mitigate | Keep offsets presentation-only and test invariant world contact across every pose. |
| T-01-11 | Denial of service | oversized/corrupt art | low | accept | Local committed files are loaded once; build/readiness checks catch malformed formats and unexpected sheet dimensions, and no remote upload surface exists. |
</threat_model>

<verification>
- Required assets: `node scripts/check-assets.mjs` validates all three exact paths, parses their PNG/WebP metadata, and confirms the sheet is 640x240.
- `npm test -- --run scripts/check-assets.test.ts src/game/penguinFrames.test.ts src/game/jump.test.ts`
- `npm run typecheck`
- `npm run build` runs the asset checker through `prebuild` before Vite.
- End-of-phase human check covers frame alignment, input acknowledgement, ramp-lip visibility, flight framing, landing contact, and rest.
</verification>

<success_criteria>
- The exact approved penguin sheet is present and rendered; no alternate character art is used.
- The snow scene uses only the selected forest and snow-pile raster assets plus drawn terrain.
- Pose changes preserve the same simulation contact point through every phase.
- Camera behavior improves readability without changing any simulation test outcome.
- Missing, empty, structurally malformed, incorrectly encoded, or wrongly sized required art stops `npm run build` before Vite.
</success_criteria>

## Artifacts this phase produces

- `package.json`: `prebuild` wiring for required-art readiness.
- `public/assets/sprites/sprite_penguin.png`: existing approved 640x240 RGBA *Where's My Egg?* character sheet consumed by this plan.
- `public/assets/sprites/winter-forest.webp`: existing selected snow background consumed by this plan.
- `public/assets/sprites/snow-pile.webp`: existing selected foreground accent consumed by this plan.
- `public/assets/sprites/README.md`: provenance, permission statement, selected-use inventory, and unused-asset policy.
- `scripts/check-assets.mjs`: required path, non-empty file, and PNG/WebP signature checks.
- `scripts/check-assets.test.ts`: subprocess fixture coverage for pass and failure readiness outcomes.
- `src/game/penguinFrames.ts`: `PenguinPose`, `PenguinFrame`, `PENGUIN_FRAMES`, and `poseForJumpPhase`.
- `src/game/penguinFrames.test.ts`: image readiness, crop-bound, phase-mapping, and contact-invariance tests.
- `src/scenes/PlayScene.ts`: approved sprite loading, custom frames, contact anchoring, curated scenery, phase pose selection, and phase-aware camera.

## Multi-Source Coverage Audit

| Source | ID | Feature or constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | - | One complete one-action jump from descent through landing and slide | 01-01, 01-02, 01-03 | COVERED |
| REQ | JUMP-01 | Repeatable automatic ramp acceleration | 01-01, 01-02 | COVERED |
| REQ | JUMP-02 | Forgiving continuous position-based quality | 01-02 | COVERED |
| REQ | JUMP-03 | Safe no-input weak hop | 01-01, 01-02 | COVERED |
| REQ | JUMP-04 | Viewport- and refresh-independent flight | 01-01, 01-02 | COVERED |
| REQ | JUMP-05 | Safe landing, slide, and rest | 01-01, 01-02 | COVERED |
| REQ | INPT-01 | Tap, click, and keyboard parity | 01-01, 01-02 | COVERED |
| REQ | INPT-02 | One consumed action per attempt | 01-01, 01-02 | COVERED |
| REQ | INPT-03 | Immediate pose and motion acknowledgement | 01-01, 01-02, 01-03 | COVERED |
| REQ | PRES-01 | Bright snow scene with readable lip/zone | 01-01, 01-03 | COVERED |
| REQ | PRES-02 | Phase-readable camera | 01-01, 01-03 | COVERED |
| REQ | PRES-03 | Approved contact-aligned character art | 01-03 | COVERED |
| RESEARCH | - | Pure fixed-step state machine and timestamped latch | 01-01, 01-02 | COVERED |
| RESEARCH | - | First-contact interpolation and clamped slide | 01-02 | COVERED |
| RESEARCH | - | Finite numeric configuration at simulation boundaries | 01-02 | COVERED |
| RESEARCH | - | Package legitimacy gate and lockfile | 01-01 | COVERED |
| RESEARCH | - | Missing or invalid selected art must fail `prebuild` before production bundling | 01-03 | COVERED |
| CONTEXT | D-01 | Broad sweet zone | 01-02 | COVERED |
| CONTEXT | D-02 | Immediate early press | 01-01, 01-02 | COVERED |
| CONTEXT | D-03 | Natural no-input hop | 01-01, 01-02 | COVERED |
| CONTEXT | D-04 | Smooth peak-at-lip curve | 01-02 | COVERED |
| CONTEXT | D-05 | User sprites in public asset path | 01-03 | COVERED |
| CONTEXT | D-06 | Snow level | 01-01, 01-03 | COVERED |
| CONTEXT | D-07 | Exact approved penguin; no substitute | 01-03 | COVERED |
| CONTEXT | D-08 | Small curated snow subset | 01-03 | COVERED |

<output>
Create `.planning/phases/01-playable-jump/01-03-SUMMARY.md` when done.
</output>
